import { expect } from "chai";
import hre from "hardhat";
import { time } from "@nomicfoundation/hardhat-network-helpers";

describe("TodoTask", () => {
    it("it should save task", async () => {

    })

    it("it should get task list", async () => { })
})